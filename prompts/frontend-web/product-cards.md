Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools, frameworks, or external assets. Focus on visual polish: spacing, typography, color, and motion.

Build an e-commerce product grid: 6 cards with CSS-illustrated product images (bottles, headphones — simple shapes), name, price, star rating, and a hover state that raises the card and slides up a "quick add" bar. One card wears a sale badge with a gentle wiggle.

Requirements:
- star ratings partially filled with a gradient trick
- quick-add bar slides from the card bottom on hover
- grid stays balanced at common widths

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
