Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools, frameworks, or external assets. Focus on visual polish: spacing, typography, color, and motion.

Build a parallax landing page with three scenes: layered CSS shape mountains that move at different scroll speeds, a headline that pins briefly, and a final CTA section that settles calmly. Keep the motion smooth and restrained — depth, not chaos.

Requirements:
- at least 3 depth layers per scene
- pinned headline releases cleanly, no jump
- works with plain scroll events or CSS scroll-driven animations

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
