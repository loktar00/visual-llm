Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools, frameworks, or external assets. Focus on visual polish: spacing, typography, color, and motion.

Build a settings page: grouped sections (Profile, Notifications, Appearance) in cards, with custom-styled toggles, radio pills, a select, and a theme accent picker of color swatches that live-updates the page's accent CSS variable. A sticky save bar appears once anything changes.

Requirements:
- toggles animate the knob and tint their track
- accent swatch selection re-colors buttons and toggles live
- save bar slides up with the changed-count

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
