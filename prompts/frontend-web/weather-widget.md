Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools, frameworks, or external assets. Focus on visual polish: spacing, typography, color, and motion.

Build a weather widget card: current temp with an animated CSS sun-behind-cloud scene, location and condition, an hourly strip, and a 5-day list with tiny animated icons (rain lines falling, sun rays rotating slowly). A toggle switches C/F with a rolling number.

Requirements:
- every icon is pure CSS, animating subtly forever
- day/night gradient background based on a fake clock
- hourly strip scrolls with snap points

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
