Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools, frameworks, or external assets. Focus on visual polish: spacing, typography, color, and motion.

Build a glassmorphic login card centered over a slowly drifting animated gradient background. Frosted-glass card with blur, soft border glow, floating labels that lift on focus, a primary button with a subtle shine sweep on hover, and a "forgot password" microlink.

Requirements:
- backdrop-filter blur with graceful fallback background
- inputs animate their labels; invalid state shakes gently
- gradient background loops seamlessly, dark jewel tones

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
