Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools, frameworks, or external assets. Focus on visual polish: spacing, typography, color, and motion.

Build a long-form article page with beautiful reading typography: a serif display headline, byline row with CSS avatar, generous measure and leading, pull quotes, a styled code block, an inline figure with caption, and a reading-progress bar fixed at the top.

Requirements:
- type scale with clear hierarchy, drop cap on the first paragraph
- progress bar fills as you scroll
- links have refined underline offsets and hover color

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
