Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools, frameworks, or external assets. Focus on visual polish: spacing, typography, color, and motion.

Build a SaaS hero section: bold headline with a gradient text sweep, a subheadline, two CTA buttons, and slow-floating soft blob shapes behind everything. A thin glass nav bar sits on top with a logo mark drawn in pure CSS.

Requirements:
- blobs drift on different periods, never harshly
- primary CTA has a gradient border shimmer on hover
- headline animates in once with a masked reveal

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
