Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools, frameworks, or external assets. Focus on visual polish: spacing, typography, color, and motion.

Build a mobile banking app screen inside a phone-shaped frame: balance header with an eye toggle that masks the amount, a horizontally scrolling row of gradient debit cards with snap, a quick-actions row of circular icon buttons, and a transactions list with CSS category icons and signed amounts.

Requirements:
- card row snaps with active-card dot indicators
- amounts color-coded, masked mode shows dots
- one transaction expands on tap for details

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
