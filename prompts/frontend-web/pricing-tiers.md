Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools, frameworks, or external assets. Focus on visual polish: spacing, typography, color, and motion.

Build a 3-tier pricing section (Starter, Pro, Enterprise) on a dark page. The middle tier is visually elevated with a "Most popular" ribbon and accent glow. Cards lift and brighten on hover; a monthly/yearly toggle animates the prices with a roll-over effect.

Requirements:
- toggle flips prices with a vertical digit roll animation
- feature lists with inline check icons drawn in CSS
- consistent 8px spacing rhythm and a clear type scale

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
