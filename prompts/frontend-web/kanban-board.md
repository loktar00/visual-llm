Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools, frameworks, or external assets. Focus on visual polish: spacing, typography, color, and motion.

Build a kanban board UI: four columns (Backlog, In progress, Review, Done) with 3-5 task cards each — labels, avatars drawn as initial circles, due-date chips. Cards tilt slightly and lift on hover; columns have subtle header counts and a ghost "add card" affordance.

Requirements:
- clean column scroll with styled scrollbars
- priority labels in distinct hues, readable in dark mode
- one card pulses softly as "urgent"

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
