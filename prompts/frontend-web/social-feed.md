Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools, frameworks, or external assets. Focus on visual polish: spacing, typography, color, and motion.

Build a social feed column: composer card at top, then post cards with CSS avatars, text, an occasional gradient "image", and an action row. The like button bursts tiny particles and bumps the count; a floating "new posts" pill appears after a few seconds and scrolls to top when clicked.

Requirements:
- like burst is small, joyful, non-blocking
- relative timestamps like "2m", "1h"
- new-posts pill slides down with a soft shadow

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
