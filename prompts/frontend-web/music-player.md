Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools, frameworks, or external assets. Focus on visual polish: spacing, typography, color, and motion.

Build a music player card: square album art rendered as a CSS gradient art piece, track title and artist, a circular progress ring around the play button that fills as a fake track "plays", skip buttons, and a volume slider with a custom thumb.

Requirements:
- play/pause morphs its icon smoothly
- progress ring animates continuously and loops the track
- background of the card picks up tints from the "art"

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
