Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools, frameworks, or external assets. Focus on visual polish: spacing, typography, color, and motion.

Build a recipe page card: hero header with a CSS-illustrated dish, meta chips (time, servings, difficulty), an ingredients checklist where checking strikes items with an animated line, step-by-step instructions with numbered markers that fill in as you check off steps, and a serving-size stepper that live-scales quantities.

Requirements:
- quantity scaling recalculates numbers in the text
- checked steps tint their number marker
- print-friendly styles via a media query

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
