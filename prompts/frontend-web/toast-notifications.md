Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools, frameworks, or external assets. Focus on visual polish: spacing, typography, color, and motion.

Build a toast notification playground: buttons trigger success, error, info, and warning toasts that stack in the bottom-right, each with an icon drawn in CSS, a message, a progress bar counting down its lifetime, and a close button. Toasts slide in, and collapse the stack gracefully when dismissed.

Requirements:
- stack reflows with animation as toasts leave
- hover pauses that toast's countdown
- distinct hue and icon per type

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
