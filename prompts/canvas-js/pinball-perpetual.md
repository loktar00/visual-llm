---
title: Perpetual Pinball
tags:
  - canvas
  - physics
---
Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools or external assets. Use a full-screen 2D canvas.

Build a self-playing pinball table: a ball launches into a table of bumpers, slingshots, ramps, a spinner, and two AI-controlled flippers that react with slight imperfection, sometimes losing the ball to drain and relaunching. Bumpers flash and score popups float upward. Physics needs proper restitution and flipper impulse. The table art should be readable, neon on dark. Camera fixed, endless play.

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
