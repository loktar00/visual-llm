---
title: Ant Colony
tags:
  - canvas
  - simulation
---
Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools or external assets. Use a full-screen 2D canvas.

Build a top-down ant colony sim: ants leave the nest, wander, find randomly spawning food piles, and carry food home laying pheromone trails that others follow and reinforce; trails evaporate over time. Render pheromone fields as soft heat, food as green clusters, the nest as a dark mound. Food depletes and respawns elsewhere so trail networks visibly form, strengthen, and collapse. No interaction.

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
