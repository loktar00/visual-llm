---
title: Boids Murmuration
tags:
  - canvas
  - flocking
---
Create a single self-contained HTML file using only HTML, CSS, and JavaScript. No build tools or external assets. Use a full-screen 2D canvas.

Build a starling murmuration screensaver: 400 to 700 boids flocking with separation, alignment, and cohesion over a dusk landscape silhouette. A slow invisible predator point occasionally splits the flock. Draw boids as small dark specks so the flock density reads like smoke. No user controls, runs forever.

Output requirement: reply with ONLY the complete HTML file — a single code block, no explanation, no commentary, not a single word outside the code.
